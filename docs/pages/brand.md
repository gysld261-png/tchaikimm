# brand 페이지 작업 기록

`test/brand_test/` 페이지의 작업 내역입니다.
대상 화면은 Figma `4조 한복판 - 차이킴` node `1707:6120`("brand_송명희", 13440 × 11260)입니다.

캔버스가 13440px로 넓은 이유는 `mood`(3장면)와 `tchaikim`(5장면) 두 섹션이
**시안에서 가로로 늘어서 있기** 때문입니다. 세로로 읽는 페이지 본체는 1920px 폭입니다.

> **`mood`는 아래 "자동 인트로로 다시 만듦" 항목이 최신입니다.**
> 1차 구현의 가로 3패널 스크롤은 페이지 진입 시 저절로 재생되는 인트로로 바뀌었습니다.
> 1차 구현 항목에서 mood에 관한 내용(섹션 구성표의 mood 행, 가로 스크롤, 검증값)은
> 더 이상 유효하지 않습니다. `tchaikim` 관련 내용은 그대로 유효합니다.

---

## 1차 구현 — 전체 레이아웃 (2026-08-07)

데스크톱 1920 기준 전체 섹션을 구현했습니다. **반응형(360 / 768 / 1280)은 다음 단계입니다**
(다른 페이지와 같은 정책).

### 변경 파일

- `test/brand_test/index.html` — 새로 만듦
- `test/brand_test/css/brand.css` — 새로 만듦
- `test/brand_test/js/brand.js` — 새로 만듦
- `test/brand_test/assets/images/` — 이미지 23 + 영상 5
- `test/brand_test/assets/icons/` — `Instagram.png`, `Facebook.png`

공통 파일은 하나도 건드리지 않았습니다.

### 에셋 정리

`asset/brand/`에 있던 25개를 `test/brand_test/assets/`로 옮겼습니다(`git mv`, 커밋 `e9be1b6`).
다른 페이지와 같은 규칙으로 `images/`(사진·영상)와 `icons/`(아이콘)로 나눴습니다.

**시안에는 있는데 내보내기에서 빠져 있던 이미지 5장을 Figma에서 직접 받아 추가했습니다.**
기존 파일이 `.jpg`인 것과 달리 이 5장은 Figma가 내보낸 원본 그대로 `.png`입니다.

```
youngjin_yellow.png   heritage_past.png
atelier_6.png         heritage_ing.png
                      heritage_now.png
```

`atelier_6`이 빠져 있어서 기존 파일이 1·2·3·4·5·7로 건너뛰고 있었습니다.

**반대로 시안 코드에서 쓰이지 않는 파일이 4개 남아 있습니다** — `mood_left.jpg`,
`brand_cta_open.jpg`, `brand_cta_close.jpg`, `brand_btn_book.jpg`.
이전 시안 버전의 잔재로 보입니다. 지우지 않고 그대로 두었습니다.

### 섹션 구성

| 섹션 | 시안 크기 | 내용 |
|---|---|---|
| `mood` | 1920 × 700 × **3장면** | 닫힌 문(검정) → 열리는 중(실내 사진 + 글) → 활짝 열림(글 + 무드 3단어) |
| `kimyoungjin` | 1920 × 2499 | 올리브→회색 그라디언트. Origin / Traditional / Handmade |
| `tchaikim` | 1920 × 1120 × **5장면** | Everyday / Forward / Seasons / Obangsaek / Woven, 각 장면에 영상 1개 |
| `atelier` | 폭 2380 | 사진 7장 + "Where craftsmanship lives." |
| `heritage` | 1920 × 1080 × 4 | "Find Your Difference" → 과거·현재·지금 사진 3장 → Bespoke 버튼 |

`mood`의 2·3장면은 **같은 글("Refined into elegance")을 반복**합니다.
2장면은 글이 오른쪽(x 1152~1920), 3장면은 왼쪽(x 0~768)에 있고 오른쪽이 무드 단어 패널로 채워집니다.
문이 열리면서 글이 왼쪽으로 밀려나고 안쪽이 드러나는 연출입니다. 시안 그대로 두 벌 배치했습니다.

### 가로 스크롤 (`js/brand.js`)

`mood`와 `tchaikim`은 화면을 붙잡아 둔 채(`pin`) 트랙을 왼쪽으로 미는 방식입니다.

- 이동 거리 = `1920 × (패널 수 − 1)`. mood 3840, tchaikim 7680.
- `scrub: 1`, `pin: true`, `invalidateOnRefresh: true`.
- **기본 HTML/CSS는 패널이 세로로 쌓인 상태**입니다. `js/brand.js`가 `is_horizontal`을
  붙였을 때만 가로로 바뀝니다. JS나 GSAP이 없어도 내용은 전부 읽힙니다.
- `gsap.matchMedia()`로 **1280px 이상 + 모션 감소 설정이 아닐 때만** 적용합니다.
  조건이 어긋나면 GSAP이 되돌리고, 정리 함수가 `is_horizontal`도 함께 뗍니다.

조절값은 `js/brand.js` 상단에 있습니다: `HORIZONTAL_MIN_WIDTH`(1280), `PANEL_WIDTH`(1920).

### 영상 5개

`preload="none"`으로 두고, `IntersectionObserver`가 `tchaikim` 섹션에 들어올 때
(`rootMargin: 200px`) 5개를 함께 재생하고 나가면 멈춥니다.
합계 22MB라 페이지 진입과 동시에 내려받지 않도록 한 것입니다.
모션 감소 설정에서는 재생하지 않습니다. 자동재생이 거부되면 첫 프레임에서 멈춘 채로 남습니다.

### 시안과 다르게 판단한 것

- **`wordmark`("TCHAI" 600px, `kimyoungjin` 안 left 80 / top 1899) 구현에서 제외**했습니다.
  Figma에서 글자 색이 `transparent`라 화면에 나타나지 않습니다.
  main 페이지의 `差 / 異 / 김영진`과 같은 판단입니다(`docs/PROJECT_CONTEXT.md`).
  **의도된 것인지 확인이 필요합니다** — 배경 이미지 채우기였다면 살려야 합니다.
- **본문 색**: 시안은 `#000000`이지만 기존 토큰 `--color_text`(#0a0a0a)를 썼습니다.
  korea 섹션·Collection과 같은 판단입니다.
- **`atelier` 사진 띠**: 폭 합계 2380px로 캔버스(1920)보다 넓습니다.
  시안대로 가운데 정렬하고 좌우를 잘라냈습니다.
- **헤더 BRAND 링크는 아직 `#`입니다.** `common/components/header.html`은 공통 파일이라
  팀에 영향이 가서 이번 작업에서는 건드리지 않았습니다.
  `data-nav-page="brand"` 덕분에 현재 페이지 표시(`is_active`)는 이미 켜집니다.
- **Bespoke 버튼**은 `../bespoke_test/index.html`로 연결했습니다(시안 주석 "비스포크로 이동").

### 아직 구현하지 않은 인터랙션

시안 주석에 적힌 연출 중 이번 1차에서 넣지 않은 것입니다. 레이아웃은 전부 자리에 있습니다.

1. **`atelier`** — "스크롤 시 사진이 한 장씩 나타난다 / 마퀴처럼 아주 천천히 자동재생"
   (참고: `corporate.pokemon.co.jp`). Collection의 As worn 띠와 같은 방식으로 만들 수 있습니다.
2. **`heritage`** — "스크롤 하면 텍스트가 커지면서 스크롤 한 번에 이미지 하나씩 오버레이"
   (참고: `flowspark-forge.webflow.io/image-background-zoom`, `motion.dev/examples/react-scroll-zoom-hero`).
3. **`tchaikim`** — "스크롤 시 화면이 나타남". 지금은 가로 이동만 있고 등장 연출이 없습니다.
4. **`kimyoungjin`** — 시안 주석의 참고 링크(`oceanease.co.kr/oceanease/brand.php`)를 아직 반영하지 않았습니다.

### 검증 결과 (2026-08-07, 1920 × 1080, localhost:5611)

- lint / test / build: 이 저장소에 설정된 도구 없음 — 실행하지 않았습니다.
- **크기가 전부 시안과 일치**: mood 패널 1920×700, `mood_open_right` 1152×700,
  `kimyoungjin_frame` 1920×2499, tchaikim 패널 1920×1120, 영상칸 1040×520,
  atelier 띠 2380, heritage 장면 1080, heritage 사진 400×714.
- **좌표 일치**: 무드 단어 3개 (171,88) (466,350) (788,229),
  Handmade (731,2199), `youngjin_group_second` (849,1199).
- **타이포 계산값 일치**: Trirong 600/64 lh 72.32 ls -1.28 · Trirong 400/24 lh 30 ·
  Montserrat 400/24 lh 37.92 · 400/20 lh 32 · 400/16 lh 26.08 ·
  Trirong 600/138 lh 155.94 ls -2.76 · Montserrat 600/11 ls 2.42.
- **색 일치**: `#120f0f`, `#585a4d`, `#1f433f`(= 기존 토큰 `--color_point`), `#262626`.
- 가로 스크롤: mood pin 0→3840에서 트랙 x가 0 / −1920 / −3840,
  tchaikim pin 7039→14719에서 0 / −3840 / −7680. 두 구간 모두 페이지 가로 스크롤 0.
- 이미지 32장 전부 로드(깨진 것 0), 에셋 요청 전부 200.
- 공통 헤더/푸터 주입 정상, 헤더 변형 `is_white`, BRAND 메뉴 `is_active`.
- 문법·구조 검사: JS `new Function()` 통과, CSS 중괄호 69/69, 파싱 규칙 69개 중
  선언 없는 규칙 0개, 중복 id 0, alt 누락 0, 이름 없는 입력요소 0.
- 콘솔 오류 없음. 초점 이동 대상 25개.
- "Find Your Difference" 1385px < 안쪽 폭 1425px — 1920에서 잘리지 않습니다.

### 확인하지 못한 부분

- **화면을 눈으로 못 봤습니다.** 이번 세션도 Browser 미리보기 패널이 표시되지 않아
  캡처가 되지 않습니다. 위 검증은 전부 DOM 측정값입니다.
  **시안 스크린샷과 나란히 놓고 보는 비교는 사용자가 해야 합니다.**
- **가로 스크롤이 좁은 화면에서 해제되는지 확인 못 했습니다.**
  이 환경은 `MediaQueryList`의 `change` 이벤트를 발생시키지 않습니다
  (768 ↔ 1920으로 바꿔도 `.matches` 값은 정확히 뒤집히는데 이벤트는 0건).
  그래서 `gsap.matchMedia()`가 되돌리는 동작을 볼 수 없었습니다.
  실제 브라우저에서 창을 줄여 확인이 필요합니다.
- `requestAnimationFrame`이 돌지 않아 실제 스크롤 감각과 영상 재생은 확인 못 했습니다.
  위 가로 이동값은 `ScrollTrigger.update()`를 직접 호출해 잰 것입니다.
- 1280px 미만에서 `heritage` 제목(1385px)이 잘립니다. 반응형 작업에서 다룹니다.

---

## mood — 자동 인트로로 다시 만듦 (2026-08-07)

**스크롤 인터랙션에서 페이지 진입 시 저절로 재생되는 인트로로 바꿨습니다.**
가로 3패널 구조를 없애고 **고정된 무대 하나** 위에서 요소가 움직이는 방식입니다.

- 검은 화면의 초록 띠가 저절로 열리며 실내가 드러납니다.
- **사진은 그 뒤로 한 번도 움직이지 않습니다.**
- 글과 무드 패널이 오른쪽에서 차례로 들어옵니다.

이전 구조에서 지적했던 "1장면과 2장면이 같은 사진이라 같은 방이 두 번 지나간다"는
문제가 사라졌습니다. 사진이 하나뿐이고 고정이기 때문입니다.

### 이게 가능한 이유 — 세 이미지의 관계

에셋을 재보고 알아낸 사실입니다.

| 파일 | 크기 | 정체 |
|---|---|---|
| `mood_start.jpg` | 30 × 484 | `mood_inner.jpg`의 **정중앙 30px 띠와 같은 픽셀** |
| `mood_left.jpg` | 768 × 700 | 벽·그림자 근접 컷 (글 뒤에 깔림) |
| `mood_inner.jpg` | 1920 × 700 | 창문이 있는 실내 전경 |

`mood_start`와 `mood_inner` 가운데 띠의 평균색을 세로 5구간으로 비교했더니
차이가 **0~8**(최대 441)이었습니다. JPEG 압축 오차 수준으로, 같은 사진입니다.

그래서 크로스페이드가 필요 없습니다. **처음부터 `mood_inner`를 깔아 두고
보이는 창만 넓히면** 그대로 이어집니다.

### 구조

```html
<section class="mood">              <!-- 배경 검정 -->
  <div class="mood_stage">          <!-- 1920 × 700 고정 무대 -->
    <div class="mood_reveal">       <!-- 열리는 창. 이 크기를 키운다 -->
      <img class="mood_room">       <!-- 실내 전경. 무대 한가운데 고정 -->
      <img class="mood_wall">       <!-- 벽 근접 컷. 나중에 나타남 -->
      <div class="mood_copy">       <!-- 글. 오른쪽에서 들어옴 -->
      <div class="mood_words">      <!-- 올리브 패널 + 3단어. 오른쪽에서 들어옴 -->
```

#### 반드시 지켜야 하는 것

1. **`.mood_room`은 `width: 100%`가 아니라 `1920px` 고정 + 가운데 정렬입니다.**
   `100%`로 두면 창이 커질 때 사진도 같이 늘어나 그냥 확대로 보입니다.
   원본 크기로 고정해야 "가려졌던 부분이 드러나는" 느낌이 납니다.
2. **`.mood_reveal`은 `translate(-50%, -50%)`로 가운데 정렬합니다.**
   그래야 `width`/`height`만 바꿔도 중심이 안 움직입니다.
3. **CSS 기본 상태가 "다 끝난 모습"입니다.** JS가 `gsap.set`으로 시작 상태를 칠한 뒤
   재생합니다. JS나 GSAP이 없으면 완성된 화면이 그대로 보입니다(점진적 향상).
4. **`initMoodIntro()`는 `DOMContentLoaded`를 기다리지 않고 바로 실행됩니다.**
   스크립트가 body 끝에 있어 mood 마크업은 이미 파싱돼 있고, 한 프레임이라도 늦으면
   완성된 화면이 번쩍였다가 시작 상태로 돌아갑니다.
5. **`.mood_reveal`의 `overflow: hidden`이 무대 밖 요소를 가려 줍니다.**
   글과 무드 패널은 시작할 때 무대 오른쪽 바깥(x 1920)에 있습니다.

### 타임라인 (총 4.2초)

| 시점(초) | 내용 |
|---|---|
| 0 → 1.4 | 창 30×484 → 1920×700. **가로가 먼저, 세로는 0.5초 뒤에** 따라붙습니다 |
| 1.9 → 2.8 | 글이 무대 밖에서 들어와 x 1152에 안착 (시안 2장면 자리) |
| 3.1 → 4.2 | 무드 패널이 들어오며 글을 x 0으로 밀어냄 (시안 3장면 자리) |
| 3.3 → 3.8 | 글 뒤의 벽 사진이 나타남 |

글과 무드 패널은 3.1~4.2 구간에서 **같은 거리(1152px)를 함께 이동**합니다.
붙어서 밀려나는 것처럼 보이는 이유입니다.

조절값은 `js/brand.js` 상단에 있습니다.

| 상수 | 현재 | 의미 |
|---|---|---|
| `OPEN_DURATION` | 1.4초 | 문이 다 열리기까지 |
| `OPEN_HEIGHT_DELAY` | 0.5초 | 가로가 먼저 벌어지고 세로가 뒤따르는 간격 |
| `COPY_IN_AT` | 1.9초 | 글이 오른쪽에서 들어오는 시점 |
| `SHIFT_AT` | 3.1초 | 글이 왼쪽으로 밀리고 무드 패널이 들어오는 시점 |

### 같이 고친 것

- 3장면 왼쪽 배경이 `mood_start.jpg`(30×484)였습니다. Figma 내보내기 코드가
  1장면과 같은 이미지 참조를 쓰고 있었기 때문인데, 그대로 두면 30px 사진을
  768px로 25배 늘려 뭉개집니다. **`mood_left.jpg`(정확히 768×700)로 바꿨습니다** — 배율 1.00.
- `mood_start.jpg`는 이제 어디에도 쓰이지 않습니다. `mood_inner`의 가운데 띠와
  같은 픽셀이라 필요가 없어졌습니다. 파일은 지우지 않았습니다.
- mood가 더 이상 스크롤을 쓰지 않아 **페이지 높이가 22789 → 18949px**로 줄었습니다.
  ScrollTrigger도 2개에서 1개(tchaikim)로 줄었습니다.

### 검증 결과 (1920 × 1080, localhost:5611)

타임라인을 직접 감아 각 시점을 측정했습니다(무대 왼쪽 위 기준 좌표).

| t | 창 | 글 | 무드 패널 | 벽 투명도 |
|---|---|---|---|---|
| 0 | 30 × 484 | 무대 밖 (1920) | 무대 밖 | 0 |
| 0.5 | 374 × 484 (가로만) | 무대 밖 | 무대 밖 | 0 |
| 1.0 | 1744 × 624 | 무대 밖 | 무대 밖 | 0 |
| 1.4 | **1920 × 700** | 무대 밖 | 무대 밖 | 0 |
| 2.4 | 고정 | x 1182 (투명도 0.96) | 무대 밖 | 0 |
| 2.8 | 고정 | **x 1152** | 무대 밖 | 0 |
| 3.7 | 고정 | x 393 | x 1161 | 0.80 |
| 4.2 | 고정 | **x 0** | **x 768** | 1.00 |

- **사진(`.mood_room`)이 전 구간 `0,0 1920×700`으로 고정** — 한 번도 움직이지 않습니다.
- 최종 위치가 시안과 일치: 글 (0, 59), 무드 패널 (768, 0),
  단어 3개 (171,88) (466,350) (788,229).
- 타임라인 길이 4.20초.
- 가로 스크롤 0, 깨진 이미지 0, 콘솔 오류 없음.
- 문법·구조 검사: JS `new Function()` 통과, CSS 중괄호 63/63, 선언 없는 규칙 0개,
  태그 짝 균형(div 53/53, section 5/5, article 8/8, figure 3/3),
  중복 id 0, alt 누락 0.

### 확인하지 못한 부분

- **재생되는 모습을 못 봤습니다.** 이 환경은 `requestAnimationFrame`이 돌지 않아
  타임라인이 저절로 진행되지 않습니다. 위 값은 `timeline.seek(t)`로 각 시점을
  직접 찍어 잰 것입니다. **실제 속도감(4.2초가 적당한지)은 사용자 확인이 필요합니다.**
- 인트로 시작 전 완성된 화면이 한 번 번쩍이는지(FOUC) 확인하지 못했습니다.
  `initMoodIntro()`를 `DOMContentLoaded` 전에 실행해 막아 두었지만,
  느린 연결에서 실제로 어떤지는 봐야 압니다.

## 다음 작업

1. 실제 브라우저에서 시안과 화면 비교
2. 위 "1장면과 2장면이 같은 사진" 문제 처리 방향 결정
3. 나머지 인터랙션 3가지 (atelier 마퀴 / heritage 오버레이 / tchaikim 등장)
4. `wordmark` 투명 텍스트 처리 방향 확인
5. 반응형(360 / 768 / 1280)
6. 헤더 BRAND 링크 연결 (공통 파일 — 팀 공지 후)
