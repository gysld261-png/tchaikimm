# Open Graph image

Place the production default image here as `og-default.jpg`.

Recommended size: 1200 × 630 px. The deployed `<meta property="og:image">` value must use the final absolute HTTPS URL.
